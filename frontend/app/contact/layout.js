export const metadata = {
    title: 'Imperial Motors - Contact & Reservations',
    description:
        'Reserve a viewing or inquire about bespoke allocations. Contact our private consultation team.',
};

export default function ContactLayout({ children }) {
    return children;
}
